# Database Operations

This directory handles the database connection and session management for the FinTrack Pro application.

- `engine.py`: Contains the setup and initialization logic for the SQLite database, including the creation of the database engine and session management.

This module ensures that the application can reliably connect to and interact with the database.